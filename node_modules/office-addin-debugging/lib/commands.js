var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const devSettings = require("office-addin-dev-settings");
const start_1 = require("./start");
const stop_1 = require("./stop");
function logErrorMessage(err) {
    console.error(`Error: ${err instanceof Error ? err.message : err}`);
}
function parseDevServerPort(optionValue) {
    const devServerPort = parseNumericCommandOption(optionValue, "--dev-server-port should specify a number.");
    if (devServerPort !== undefined) {
        if ((devServerPort < 0) || (devServerPort > 65535)) {
            throw new Error("--dev-server-port should be between 0 and 65535.");
        }
    }
    return devServerPort;
}
function parseNumericCommandOption(optionValue, errorMessage = "The value should be a number.") {
    switch (typeof (optionValue)) {
        case "number": {
            return optionValue;
        }
        case "string": {
            let result;
            try {
                result = parseInt(optionValue, 10);
            }
            catch (err) {
                throw new Error(errorMessage);
            }
            if (Number.isNaN(result)) {
                throw new Error(errorMessage);
            }
            return result;
        }
        case "undefined": {
            return undefined;
        }
        default: {
            throw new Error(errorMessage);
        }
    }
}
function start(manifestPath, command) {
    return __awaiter(this, void 0, void 0, function* () {
        try {
            const sourceBundleUrlComponents = new devSettings.SourceBundleUrlComponents(command.sourceBundleUrlHost, command.sourceBundleUrlPort, command.sourceBundleUrlPath, command.sourceBundleUrlExtension);
            const debuggingMethod = start_1.parseDebuggingMethod(command.debugMethod);
            const devServerPort = parseDevServerPort(command.devServerPort);
            const enableLiveReload = (command.liveReload === true);
            yield start_1.startDebugging(manifestPath, debuggingMethod, sourceBundleUrlComponents, command.devServer, devServerPort, command.packager, command.packagerHost, command.PackagerPort, command.sideload, command.debug, enableLiveReload);
        }
        catch (err) {
            logErrorMessage(`Unable to start debugging.\n${err}`);
        }
    });
}
exports.start = start;
function stop(manifestPath, command) {
    return __awaiter(this, void 0, void 0, function* () {
        try {
            yield stop_1.stopDebugging(manifestPath, command.unload);
        }
        catch (err) {
            logErrorMessage(`Unable to stop debugging.\n${err}`);
        }
    });
}
exports.stop = stop;
//# sourceMappingURL=commands.js.map